# to install any library we need to use "pip install library_name".
# to import them use "import library_name".
# "pip list" gibes u entire python libraries installed on vscode.
# venv = virtual environment.

import pandas 
import numpy
